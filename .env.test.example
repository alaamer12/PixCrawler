# PixCrawler Test Environment Configuration
# This file serves as a template for .env.test (which is gitignored)
# Copy this file to .env.test and update with your actual test values

# =============================================================================
# ENVIRONMENT CONFIGURATION
# =============================================================================
PIXCRAWLER_ENVIRONMENT=testing
PIXCRAWLER_DEVELOPMENT_MODE=false
PIXCRAWLER_DEBUG=false
PIXCRAWLER_LOG_LEVEL=INFO

# =============================================================================
# DATABASE CONFIGURATION (PostgreSQL)
# =============================================================================
# Local development: Use your local PostgreSQL instance
# CI/CD: This will be replaced with GitHub Actions service
DATABASE_URL=postgresql://test_user:test_password@localhost:5432/pixcrawler_test
DATABASE_POOL_SIZE=5
DATABASE_MAX_OVERFLOW=10

# =============================================================================
# SUPABASE CONFIGURATION
# =============================================================================
# For testing, you can use test/staging Supabase project or mock values
# CI/CD: These will be populated from GitHub Secrets
SUPABASE_URL=https://your-test-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_test_service_role_key_here_minimum_50_characters_required_for_validation
SUPABASE_ANON_KEY=your_test_anon_key_here_minimum_50_characters_required_for_validation_purposes

# =============================================================================
# REDIS CONFIGURATION
# =============================================================================
# Local development: Use your local Redis instance
# CI/CD: This will be replaced with GitHub Actions service
REDIS_URL=redis://localhost:6379/0
REDIS_EXPIRE_SECONDS=3600

# =============================================================================
# SECURITY CONFIGURATION
# =============================================================================
# Use test-specific secret key (NOT your production key)
SECURITY_SECRET_KEY=test-secret-key-change-this-to-a-random-string-min-32-chars
SECURITY_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8000
SECURITY_JWT_EXPIRE_MINUTES=60

# =============================================================================
# STORAGE CONFIGURATION
# =============================================================================
# For tests, typically use local storage
STORAGE_PROVIDER=local
STORAGE_LOCAL_ROOT=./test_storage
STORAGE_LOCAL_BASE_URL=http://localhost:8000/storage

# =============================================================================
# RATE LIMITING CONFIGURATION
# =============================================================================
RATE_LIMIT_ENABLED=false
RATE_LIMIT_PER_MINUTE=60

# =============================================================================
# CELERY CONFIGURATION
# =============================================================================
# For tests, you may want to use eager mode (synchronous execution)
CELERY_BROKER_URL=redis://localhost:6379/1
CELERY_RESULT_BACKEND=redis://localhost:6379/1
CELERY_TASK_ALWAYS_EAGER=true

# =============================================================================
# NOTES
# =============================================================================
# 1. Copy this file: cp .env.test.example .env.test
# 2. Update .env.test with your actual test credentials
# 3. Never commit .env.test to version control
# 4. For CI/CD, these values are set via GitHub Secrets
