add manage.py file and make it works in this env [also remmeber to add a function to start server in background without blocking main process]
ensure all endpoints are working
document authorized endpoints in docstrings and using tags
ensure all endpoints are testable and should work with Postman

re-create the azure script using python
test all apis working manually using `-Invoke Request` from powershell

ensure consistency between .env, and best practice, the global .env should be handling global project context without the pakcages-env file know about it or vice versa

install production dependencies like guincorn

ensure our celery tasks and workflows are used with best practices

re-hierachy tests [just moving files, but not creating more because we are close to deadline]

complete the sdk code

fix ci/cd with our windx ci/cd
