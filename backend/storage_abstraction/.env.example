# Environment selection
STORAGE_ENV=dev

# Force provider per tier (optional)
# STORAGE_TEMP_PROVIDER=local
# STORAGE_HOT_PROVIDER=local
# STORAGE_COLD_PROVIDER=local

# Local provider
LOCAL_BASE_PATH=./storage_data
# LOCAL_PUBLIC_BASE_URL=http://localhost:8000/storage

# S3 provider (optional)
# S3_BUCKET=your-shared-bucket
# S3_BUCKET_TEMP=your-temp-bucket
# S3_BUCKET_HOT=your-hot-bucket
# S3_BUCKET_COLD=your-cold-bucket
# S3_REGION=us-east-1
# S3_PUBLIC_BASE_URL=https://cdn.example.com
# S3_PREFIX=prod/app
